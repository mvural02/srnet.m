# SRNet

This repository contains a MATLAB script (`srnet.m`) for training a deep residual neural network (ResNet) on an imag The code is designed to load grayscale images, build a custom ResNet architecture, and train it using the Adam optimizer.
e dataset. The code is designed to load grayscale images, build a custom ResNet architecture, and train it using the Adam optimizer.
